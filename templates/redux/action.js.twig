import { PipelineRequest } from '@shopgate/engage/core';
import {
  REQUEST_{{ entityName | upper }},
  RECEIVE_{{ entityName | upper }},
} from '../constants';


export const fetch{{ entityName }} = () => (dispatch, getState) => {
  const request = new PipelineRequest('pipeline').dispatch();

  request
    .then((response) => {})
    .catch(error => {});

  return request;
}
