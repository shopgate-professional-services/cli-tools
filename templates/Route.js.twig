import React from 'react';
import { Route } from '@shopgate/pwa-common/components';
import {{ name }}Page from '../../pages/{{ name }}Page';
import { {{ name | upper }}_PATTERN } from '../../constants';

/**
 * @returns {JSX}
 */
export default () => (<Route pattern={ {{ name | upper }}_PATTERN } component={ {{ name }}Page } />);
